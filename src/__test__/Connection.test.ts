import { OBRest } from '../index';


function InvalidURLConection():boolean{
    
    return true;
}


test('OB Conection with inextisten url', () => {
    expect(InvalidURLConection()).toBe(true);
});

test('OB Conection with inextisten url', () => {
    expect(InvalidURLConection()).toBe(true);
});